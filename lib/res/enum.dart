// app enum
enum FilterType {
  all,
  favorite,
  archived,
  discover,
  uploaded,
}

enum CategoryTypeEnum {
  BOOK_TYPE,
  BOOK_CATEGORY,
}

enum ScanFormatEnum {
  pdf,
  word,
  image,
}
enum SnackBarType {
  success,
  error,
  warning,
  info,
  custom,
}
enum EmptyDataEnum {
  no_data,
  no_filter
}